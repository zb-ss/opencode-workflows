---
description: Commit the recent changes
agent: build
model_tier: low
---

Here is the current status and the staged diff:

Status:
!`git status -s`

Diff:
!`git diff --staged`

Based only on the diff provided above, write a concise and descriptive commit message that follows the Conventional Commits specification.
And commit the changes but do not push them to remote.
DO NOT DO ANYTHING ELSE, JUST FOCUS ON THIS AND LET ME KNOW IF THE COMMIT WAS SUCCESSFUL!
